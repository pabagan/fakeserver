{
  "name": "fake-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "yarn run fakeserver",
    "fakeserver": "json-server --watch db.js --port 8080 --routes routes.json"
  },
  "dependencies": {
    "dotenv": "^4.0.0",
    "faker": "^4.1.0",
    "json-server": "^0.12.1"
  }
}
